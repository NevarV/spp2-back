spring:
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: url
    username: username
    password: password
    hikari:
      maximum-pool-size: 2
  jpa:
    hibernate:
      ddl-auto: update
nevar:
  app:
    jwtSecret: nevarSecretKey
    jwtExpirationMs: 86400000